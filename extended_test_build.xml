<?xml version="1.0" encoding="UTF-8"?>
<project>
    <if condition="${test.enable}">
        <then>
            <import file="${subscripts.test.project.directory}/coverage-xml.xml" />
        </then>
    </if>
</project>
